services:
  backend:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      - SPRING_DATA_REDIS_HOST=redis
    depends_on:
      - db
      - redis
  frontend:
    build: ./frontend
    ports:
      - "4200:4200"
  db:
    image: postgres:latest
  redis:
    image: redis:latest